---
interface Props {
	heading: string;
	description?: string;
	eyebrow?: string;
}

const { heading, description, eyebrow } = Astro.props;
---

<header class="page-header">
	{eyebrow && <p>{eyebrow}</p>}
	<h1>{heading}</h1>
	{description && <p>{description}</p>}
</header>
